# Default example

This deploys the module with its default secure configuration: a Linux App Service Plan with a single web app, VNet integration with private endpoints, private DNS zone, and Azure Front Door Premium with WAF.
